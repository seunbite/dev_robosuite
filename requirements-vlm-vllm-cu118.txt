# Pinned vLLM stack for old NVIDIA drivers (install via scripts/install_vlm_vllm_cu118.sh)
# Do NOT pip install bare "vllm" — it pulls torch+cu124 and breaks on Babel nodes.
#
# torch==2.4.0+cu118  (or cu121 if your driver supports it)
# vllm==0.8.5          (Qwen2.5-VL OK; avoids vLLM 0.22 v1 engine + new CUDA)

vllm==0.8.5
transformers>=4.45.0
qwen-vl-utils>=0.0.8
accelerate>=0.30.0
openai>=1.40.0
httpx>=0.27.0
PyYAML>=6.0

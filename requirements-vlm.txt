# Extra deps for pilot VLM experiments (install on top of robosuite env)
# vLLM itself: pip install vllm  OR  bash scripts/start_vllm_server.sh (auto-installs)
openai>=1.40.0
httpx>=0.27.0
google-genai>=1.0.0
PyYAML>=6.0

# Pilot VLM experiments
#
# Babel / old NVIDIA driver (recommended):
#   bash scripts/install_vlm_transformers.sh
#   BACKEND=transformers
#
# New GPU + vLLM (optional):
#   pip install vllm>=0.8.0
#   BACKEND=vllm

transformers>=4.45.0
accelerate>=0.30.0
qwen-vl-utils>=0.0.8
openai>=1.40.0
httpx>=0.27.0
google-genai>=1.0.0
PyYAML>=6.0
